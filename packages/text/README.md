# text/ — text utility tools

English | [中文](README.zh.md)

Small stateless model-facing text utilities that need no capability seam: each package registers one pure tool on `ctx.tools`.

| Package | Role | ctx key |
|---|---|---|
| [`tool-text/`](tool-text/README.md) | Pure text measurements (`text_stats`). | (registers on `ctx.tools`) |
